{
    "name": "Spotify Auth Heroku",
    "description": "Handles the access token exchange for Spotify callbacks to simplify workflow.",
    "env": {
        "CLIENT_ID": "{SET_FROM_SPOTIFY}",
        "CLIENT_SECRET": "{SET_FROM_SPOTIFY}",
        "CALLBACK_URL": "https://{SET THIS}.herokuapp.com",
        "REDIRECT_URL": "https://{SET_THIS}"
    }
}